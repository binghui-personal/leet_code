# leet_code
Personal leet code test

# build
$ cd /path/to/code
$ mkdir build && cd build
## 使用VS 2022编译
$ cmake -G "Visual Studio 17 2022" .. && cmake --build .
## 使用VS 2019编译
$ cmake -G "Visual Studio 17 2022" -T v142 .. && cmake --build .

